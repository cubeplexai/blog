---
slug: yc-qm-multiplayer-agent-harness
title: Quartermaster（QM）：YC 开源的 Multiplayer Agent Harness
description: YC 在管理 50 多个个人 Agent 时遇到了协作与运维问题。开源的 QM 将 Slack 的人、频道、群组和项目解析为 Scope，并把环境、访问关系和持久 sandbox 置于相应边界内。
authors: [xfgong]
tags: [Quartermaster, QM, Agent Harness, Multiplayer Agents, YC, Open Source]
image: /img/blog/yc-qm-multiplayer-agent-harness-cover.webp
---

2026 年 7 月，YC 开源了 QM（Quartermaster）。YC 在[发布说明](https://qm.ycombinator.com/)中回顾，团队先做过一个能访问内部数据的 Ruby agent loop，后来加上 cron 和 webhook；再后来，他们给员工配了 50 多个 Hermes 个人 Agent。Agent 很灵活，但管理这样一队 Agent 已经变得麻烦。

QM 是这段经验之后的产物。它希望保留个人 Agent 的灵活性，同时让组织能够管理模型、权限和运行环境。Slack 私聊、频道、多人私信和项目会映射到不同 Scope；协作发生在这些 Scope 内，而不是把一个人的环境直接交给整个公司。

<!-- truncate -->

## YC 遇到的管理问题

早期的 Ruby loop 很容易让员工上手，但能做的事情有限。补上 cron 和 webhook 后，Agent 可以在用户不在线时继续工作。个人 Agent 增加后，管理重点从单个任务转到整批 Agent 的权限、配置和运行方式。

YC 想要 Hermes 的灵活性，也想找回早期系统的简单。发布说明还提到，他们希望拥有并自行托管这套系统。QM 因此面向 startup：它先给个人和项目划出工作空间，再为协作和组织治理提供共同的规则。

## Scope 如何组织个人与共享工作

QM 用 Scope 表示工作边界。个人、频道、团队、组织和群组都有各自的 Scope；项目使用 group 类 Scope。Slack 私聊解析到个人 Scope，普通频道解析到频道 Scope，多人私信解析到群组 Scope。项目还可以关联 Slack 主页频道，频道成员随之成为项目成员，频道成为计划任务和汇报的默认投递位置。

Scope 决定谁能进入环境、哪些文件层可见、凭据如何授权，以及命令发往哪个持久 sandbox。个人 Scope 默认不会把另一位成员的文件或凭据带入运行；频道和群组 Scope 则为其成员提供共同的协作边界。组织、当前 Scope 与个人 Scope 可以共同参与解析，但它们的读写边界不同。

Skill 归某个 Scope 所有，需要通过 grant 才能交给另一个 Scope 使用；推广到全组织由管理员决定。个人经验、频道协作、项目工作和组织资产因此有不同的归属和传播路径。

组织也保留了一层明确的责任。它决定可用的 harness 与模型，选择安全姿态，并管理全组织可见的 Skill。管理员无需把个人凭据和私人上下文当作公共资源；需要共享的文件、Skill 或访问关系必须在对应 Scope 之间明确授予。

## 通用核心与组织定制

这些边界需要运行时配合。QM 的 headless core 集中处理 API、身份、策略、调度和 agent loop，Postgres 保存持久状态。命令通过 `execute` 进入对应 scope 的 sandbox；工具安装在 sandbox 后会保留，成为该 scope 的长期工作环境。

QM 把通用 core 与组织定制分开。组织配置、自定义 tools 和 skills、sandbox 镜像及基础设施放在 deployment directory，由 `qm init` 部署到运营方自己的云账号。README 还给出私有 fork 的维护方式：组织把私有内容留在 `deploy/layers/`，让 core 保持与上游一致；`update-qm` 用于合并上游并创建同步 PR，`upstream-pr` 用于把不含组织信息的修复送回 QM。这样，组织可以维护自己的 Agent 环境，也能继续跟上公共项目。

Web、Admin 和 Portal 都是 core HTTP API 上的插件；Slack 由 core 直接启动和监督。对使用者而言，这意味着个人和团队可以在 Slack 与 Web 间延续同一身份和配置；对运营者而言，身份、策略和审计仍留在同一处控制。

## 文档写明的限制

QM 仍是 early、experimental 软件，并非已经硬化的公有或多租户服务边界。它的 command policy 能拦住一些危险命令，但混淆、编码或“先写后执行”仍可能绕过规则。sandbox 中正在使用的凭据是明文；内容筛选也不完整。管理员读取敏感内容会留下审计记录，但不需要再次征得用户同意。

这决定了 QM 的适用范围。它给团队提供了一套管理个人 Agent 和共享工作的方式，不替代部署方自己的安全与运维审查。浏览器 runner 也不重新经过 command policy 或人工审批，部署方需要单独评估这条路径。

## 来源

- [YC 的 QM 发布说明](https://qm.ycombinator.com/)
- [QM README（提交 `1a6b0e4`）](https://github.com/yc-software/qm/blob/1a6b0e443016079b7e79ac874a8cfda0a5f3b306/README.md)
- [Scope 类型与解析（提交 `1a6b0e4`）](https://github.com/yc-software/qm/blob/1a6b0e443016079b7e79ac874a8cfda0a5f3b306/src/types.ts) 与 [Scope membership](https://github.com/yc-software/qm/blob/1a6b0e443016079b7e79ac874a8cfda0a5f3b306/src/resolution/scope-membership.ts)
- [SECURITY.md（提交 `1a6b0e4`）](https://github.com/yc-software/qm/blob/1a6b0e443016079b7e79ac874a8cfda0a5f3b306/SECURITY.md)
